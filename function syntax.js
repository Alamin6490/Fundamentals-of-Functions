// function declear  
    
 
const number = 45;

function fanOffKor(){
    console.log('basa thake uthe  dara');
    console.log('walk kor');
    console.log('click the switch to off of the Fan');

}

// call the function 

fanOffKor();
//explain the code

```javascript
const number = 45; // number নামক একটি ভেরিয়েবল যেখানে 45 সংরক্ষিত হয়েছে

// fanOffKor ফাংশনটি ফ্যান বন্ধ করার প্রক্রিয়াটি বুঝিয়ে দিচ্ছে
function fanOffKor() {
    console.log('basa thake uthe dara'); // বসা থেকে উঠে দাঁড়ানোর কাজ
    console.log('walk kor'); // হাঁটা শুরু করার নির্দেশ
    console.log('click the switch to off of the Fan'); // ফ্যানের সুইচ বন্ধ করার কাজ
}

// ফাংশনটি কল করা হচ্ছে, যাতে ফ্যান বন্ধ করার প্রক্রিয়াটি চালানো হয়
fanOffKor();
``` 

এখানে ফাংশনটি মূলত তিনটি কাজের লজিক্যাল স্টেপ দেখাচ্ছে, যেমন বসা থেকে দাঁড়ানো, হাঁটা এবং ফ্যানের সুইচ বন্ধ করা।


























ফাংশন (Function) হলো জাভাস্ক্রিপ্টের একটি মৌলিক ধারণা, যা কোডের পুনরায় ব্যবহারযোগ্য অংশ তৈরি করতে সাহায্য করে। সহজভাবে বলতে গেলে, ফাংশন হলো এমন একটি কোড ব্লক যা নির্দিষ্ট কাজ সম্পাদন করে।

### ফাংশন এর ভূমিকা:
1. **কোড পুনরায় ব্যবহার করা যায়**: একবার ফাংশন তৈরি করলে, সেটি বিভিন্ন স্থানে ব্যবহার করা যায়।
2. **কোডকে গুছিয়ে রাখা যায়**: বড় বড় কোডকে ছোট ছোট অংশে ভেঙে রাখা যায়, ফলে কোড পড়া এবং বোঝা সহজ হয়।
3. **প্যারামিটার নিয়ে কাজ করে**: ফাংশনে ডেটা (প্যারামিটার) পাঠিয়ে, বিভিন্ন রকমের কাজ করানো যায়।

### ফাংশন লেখার নিয়ম (সিনট্যাক্স):

জাভাস্ক্রিপ্টে ফাংশন লেখার নিয়ম হলো:

```javascript
function functionName(parameters) {
  // ফাংশনের ভিতরে যা কোড থাকবে
  return result; // এই অংশটি ঐচ্ছিক, এটি ফাংশন থেকে কোনো মান ফেরত দিতে ব্যবহৃত হয়
}
```

#### উদাহরণ:

```javascript
function sayHello(name) {
  console.log("Hello, " + name + "!");
}

sayHello("Rahim"); // Output: Hello, Rahim!
```

এখানে:
- **function**: এটি দিয়ে ফাংশন শুরু করা হয়।
- **functionName**: এটি ফাংশনের নাম, যেটা দিয়ে ফাংশনকে ডাকা হবে।
- **parameters**: এগুলো ফাংশনে ডেটা পাঠানোর জন্য ব্যবহার করা হয়।
- **return**: ফাংশন থেকে কোনো ফলাফল ফেরত দেওয়ার জন্য ব্যবহার করা হয়। 

ফাংশনকে ব্যবহার করার জন্য সেটিকে "কল" করতে হয়, যেমন উপরের উদাহরণে `sayHello("Rahim")`।

এইভাবে জাভাস্ক্রিপ্টে ফাংশন কাজ করে।
